version: 1.1
runtime: corretto11
build:
  commands:
    build:
      - ./gradlew build
run:
  command: java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005 -jar build/libs/demo-0.0.1-SNAPSHOT.jar
  network:
    ports:
      - "8080:8080"
